# Database Configuration
# For Vercel Postgres: Copy the connection string from your Vercel dashboard
# It includes built-in connection pooling automatically
DATABASE_URL="postgres://user:password@host:port/db-name"

# Optional: Add connection pooling parameters for external PostgreSQL databases
# These parameters are ignored by Vercel Postgres (which handles pooling automatically)
# For self-hosted or other providers, you can add these to the DATABASE_URL:
#   ?connection_limit=20&statement_cache_size=20
# Example: DATABASE_URL="postgres://user:pass@localhost:5432/portfolio?sslmode=require&connection_limit=20"

# Email Configuration (for contact form)
# Required in production — do not use "console" on Vercel/production
EMAIL_SERVICE="resend"
# Options: console (dev only), resend, sendgrid, smtp

EMAIL_FROM="your-email@example.com"
CONTACT_EMAIL="your-email@example.com"
EMAIL_TO="your-email@example.com"

# Resend (when EMAIL_SERVICE=resend)
RESEND_API_KEY="re_xxxxxxxx"

# SendGrid (when EMAIL_SERVICE=sendgrid)
SENDGRID_API_KEY="SG.xxxxxxxx"

# SMTP (when EMAIL_SERVICE=smtp — not yet implemented in app)
EMAIL_SMTP_HOST="smtp.example.com"
EMAIL_SMTP_PORT="587"
EMAIL_SMTP_USER="your-username"
EMAIL_SMTP_PASSWORD="your-password"

# Grafana Cloud OTLP — separate endpoints for traces and metrics
# Copy the OTLP gateway host from Grafana Cloud → your stack → Configure OpenTelemetry.
# Use the regional gateway (e.g. otlp-gateway-prod-us-east-3.grafana.net), NOT your stack UI hostname.
# OTEL_EXPORTER_OTLP_TRACES_ENDPOINT="https://otlp-gateway-prod-us-east-3.grafana.net/otlp/v1/traces"
# OTEL_EXPORTER_OTLP_TRACES_HEADERS="Authorization=Basic <base64-user:token>"
# OTEL_EXPORTER_OTLP_METRICS_ENDPOINT="https://otlp-gateway-prod-us-east-3.grafana.net/otlp/v1/metrics"
# OTEL_EXPORTER_OTLP_METRICS_HEADERS="Authorization=Basic <base64-user:token>"
