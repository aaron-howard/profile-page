# Design assets and large binaries

Do not commit large design exports (Figma zips, Sketch files, raw `stitch_profile` bundles, etc.) to this repository. Keep them in cloud storage or your design tool, and link to them from internal docs if needed.

The repo previously included `stitch_profile.zip`; it was removed to keep clones and CI fast. Regenerate or store that archive outside git.
